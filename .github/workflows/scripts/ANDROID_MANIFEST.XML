import re

manifest_match = re.search(r"<manifest\b[^>]*>", s)
if manifest_match is None:
    raise SystemExit("Could not find Android <manifest> tag.")

insert_at = manifest_match.end()
s = s[:insert_at] + insertion + s[insert_at:]
